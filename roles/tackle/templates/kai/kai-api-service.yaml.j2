---
apiVersion: v1
kind: Service
metadata:
  name: "{{ kai_service_name }}"
  namespace: "{{ app_namespace }}"
spec:
  selector:
    app: kai-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
