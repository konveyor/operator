---
apiVersion: v1
kind: Service
metadata:
  name: kai-hub-importer
  namespace: "{{ app_namespace }}"
spec:
  selector:
    app: kai-hub-importer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
